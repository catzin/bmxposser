export interface marca{
    marca: string;

}

export interface upload{
    ok:boolean;
    msg:string;
}

export interface msg{
    ok:boolean;
    msg:string;
}

export interface categoria{
    categoria:string;
}